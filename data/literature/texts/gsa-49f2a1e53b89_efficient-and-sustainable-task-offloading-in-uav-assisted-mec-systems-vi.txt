[page 1]
Efficient and Sustainable Task Offloading in UA V-Assisted MEC Systems via Meta Deep Reinforcement Learning Maryam Farajzadeh Dehkordi Department of Electrical and Computer Engineering George Mason University Fairfax, V A, USA mfarajza@gmu.edu Bijan Jabbari Department of Electrical and Computer Engineering George Mason University Fairfax, V A, USA bjabbari@gmu.edu Abstract—Integrated into existing Mobile Edge Computing (MEC) systems, Unmanned Aerial Vehicles (UA Vs) serve as a cornerstone in meeting the stringent requirements of future Internet of Things (IoT) networks. The current endeavor studies an MEC system, in which a computationally-empowered UA V , wirelessly linked to a cloud server, is destined for task offloading in uplink transmission of IoT devices. The performance of this system is studied by formulating a resource allocation problem, which aims to maximize the long-term computed task efficiency, while ensuring the stability of task buffers at the IoT devices, UA V and cloud. The problem jointly optimizes the uplink transmit power of IoT devices and their offloading decisions, the trajectory of the UA V and computing power at all transceivers. Regarding the non-convex and stochastic nature of the problem, we devise a multi-step solution approach. Initially, by invoking the fractional programming and Lyapunov theory, we transform the longterm optimization problem into an equivalent per-time-slot form. Subsequently, we recast the reformulated problem as a Markov Decision Process (MDP), which reflects the network dynamics. The MDP model, eventually, serves for training a Meta Twin Delayed Deep Deterministic Policy Gradient (MTD3) agent, in charge of adaptive resource allocation with respect to the MEC system variations derived from the mobility of the UA V and IoT devices. Simulations reveal the dominance of our proposed resource allocation approach over its Deep Reinforcement Learning (DRL)powered counterparts, increasing computed task efficiency and reducing task buffer lengths. Index Terms—Deep Reinforcement Learning (DRL), Internet of Things (IoT), Markov Decision Process (MDP), Mobile Edge Computing (MEC), Unmanned Aerial Vehicle (UA V), task offloading, Meta Twin Delayed Deep Deterministic Policy Gradient (MTD3). I. I NTRODUCTION The rapid growth of Internet of Things (IoT) technology has significantly escalated the need for computational power, especially for low-delay applications like autonomous vehicles, where failure is not even an option at scale. However, many IoT devices lack the necessary computing capabilities. Mobile Edge Computing (MEC) systems address this limitation by allowing these devices to offload tasks to nearby, more powerful edge nodes [1]. Unmanned Aerial Vehicles (UA Vs) further enhance MEC by offering mobility and flexible deployment. In this context, an efficient resource allocation structure plays a crucial role in managing the demands in a timely approach, which is essential for meeting the stringent requirements of IoT applications. In this regard, extensive research has examined UA V-assisted MEC systems [2]–[7]. Although providing valuable insights, existing work often overlooks the long-term challenges of non-deterministic conditions in these networks. Recent studies address this by exploring UA V-enabled networks through a long-term causality lens, incorporating Lyapunov optimization and then successive convex approximation [4]–[6]. However, conventional algorithms can be slow, computationally heavy, and less flexible in dynamic settings. To overcome these limitations, advanced reinforcement learning approaches, have been proposed to enhance adaptability and efficiency in realtime UA V operations. In this paper, we propose a three-tier UA V-assisted MEC system with cloud, UA V , and IoT devices tiers managing computational task queues, extending our previous work [8]. Unlike prior research focused on short-term metrics, this study emphasizes long-term system performance through a new metric—computed task efficiency, defined as the ratio of time-averaged computed task to transmission latency. To maintain long-term stability, we use fractional programming [9] and Lyapunov optimization [10], techniques applied effectively in previous studies [4]–[6]. However, rather than their complex algorithms, we reformulate the problem as a Markov Decision Process (MDP) and solve it using Deep Reinforcement Learning (DRL) technique that offer an efficient alternative, enabling effective sequential decision-making under uncertainty. Furthermore, to overcome the challenges of limited scalability and poor generalizability associated with deep Qnetworks [8], and to enhance accuracy through continuous decision variables, by integrating Meta learning [11] with Twin Delayed Deep Deterministic Policy Gradient (TD3) [12], in this paper we introduce Meta Twin Delayed Deep Deterministic Policy Gradient (MTD3) algorithm for UA V-assisted MEC systems. This algorithm optimally balances system utility and stability, adapting effectively to the dynamic movement of UA V . Simulation results show that our resource allocation scheme improves system performance, increasing computed arXiv:2504.00453v1 [eess.SP] 1 Apr 2025

[page 2]
Fig. 1: Illustration of a UA V-assisted MEC system task efficiency and reducing queue lengths. II. S YSTEM SETUP AND RESOURCE ALLOCATION As illustrated in Fig. 1, the MEC system comprises a set K = {1, 2, . . . , K} of K IoT devices, each with limited computing power denoted by Cmax k , necessitating efficient offloading mechanism for computational tasks. To support this, the MEC network incorporates a cloud infrastructure with high computational capacity denoted by CC, max capable of computing these tasks. However, the cloud’s considerable distance from the IoT devices introduces communication challenges. To address this, a UA V equipped with computation as well as communication resources is deployed to act as both a relay for re-offloading tasks to the cloud and as an independent computing unit with computational power capacity denoted by CU, max. The system, therefore, follows a three-tier structure: each IoT device computes part of its task locally, offloads another portion to the UA V through via Orthogonal Frequency Division Multiple Access (OFDMA), and queues any remaining tasks. Next, the UA V , in turn, computes part of the offloaded task, re-offloads another portion to the cloud, and retains the rest in its queue. Subsequently, the cloud computes a portion of the re-offloaded tasks and queues any remaining ones. Each device is equipped with a single antenna, and the UA V-to-cloud link operates with sufficient bandwidth. A. Communication Model The UA V’s mission duration T is divided into N time slots, each lasting T N = τ, where each time slot n ∈ N = {1, 2, . . . , N}. To ensure quasi-static wireless channels within each time slot, we adopt a block fading channel model, in which channel conditions remain constant throughout a slot but may vary independently between successive time slots. Additionally, we assume perfect channel state information, achievable through established estimation methods [13]. The uplink channels experience both large-scale path loss and small-scale Rician fading with a Rician factor KR. For the kth IoT device in the nth time slot with horizontal location at [xk[n], yk[n]], positioned at a distance dU k[n] from the UA V , the distance is given by dU k[n] = p (xk[n] − xU[n])2 + (yk[n] − yU[n])2 + H 2, where the UA V’s location is denoted by [xU[n], yU[n], H]. The channel loss is then modeled as hU k[n] =q η0 dU k[n]θ q KR KR+1 ρLoS k [n]+ q 1 KR+1 ρNLoS k [n]  , [14], where η0 denotes the average channel power gain at the reference distance d0 = 1 m, and θ represents the path loss exponent. As such, the UA V receives signals offloaded by all IoT devices at the nth time slot, represented as w[n] = P k∈K hU k[n] p pk[n]uk[n] + zU. Here, uk[n] is the unit-power information symbol for the kth IoT device at the nth time slot, and pk[n] denotes the corresponding uplink transmit power. Additionally, zU ∼ CN (0, σ2 z) represents the additive white Gaussian noise at the UA V , with zero mean and variance σ2 z. Accordingly, the signal-to-interference-plusnoise ratio for the signal sent by the kth IoT device and received by the UA V via OFDMA during this time slot is expressed as γk[n] = |hU k[n]| 2 pk[n] PK i=1 i̸=k |hU k[n]| 2 pi[n]+σ2z . Following the Shannon-Hartley theorem, the maximum achievable task offload rate for the kth IoT device at the nth time slot is calculated as Rk[n] = B0 log2 (1 + γk[n]), where B0 denotes the communication channel bandwidth in Hertz. Then, the transmission latency for offloading the task from the IoT device to the UA V is given by tU, Comm k [n] = xU k[n]QL k[n] Rk[n] , where Qk represents the backlog at local tier for the kth IoT device at the nth time slot and xU k[n] denotes the task offloading indicator from the the kth IoT device to the UA V at the nth. Accordingly, the numerator represents the volume of task (in bits) offloaded from the IoT device to the UA V . The transmission latency for re-offloading a task from the UA V to the cloud, denoted by tC, Comm k [n], is given by t0, a fixed time for establishing a high-speed communication link between the UA V and the cloud, provided by xC k[n] > 0; otherwise, it is 0. Here, xC k[n] is an indicator variable that determines the portion of the kth IoT device’s task at the nth time slot is further re-offloaded from the UA V to the cloud. Accordingly, the total transmission latency for the kth IoT device’s task at the nth time slot, denoted as tComm k [n], is the sum of these transmission latencies calculated as tComm k [n] = tU, Comm k [n] + tC, Comm k [n]. Then, the system transmission latency at the nth time slot is given by tComm[n] =P k∈K tComm k [n]. B. Computation Model To compute the tasks efficiently, each IoT device computes part of its task locally, offloads another portion to the UA V , and queues any remaining portion. Next, the UA V , in turn, computes part of the offloaded task, re-offloads another portion to the cloud, and retains the rest in its queue. Subsequently, if additional computing is required, the tasks are then reoffloaded to the cloud to complete computation, where the cloud computes a portion of the re-offloaded tasks and queues any remaining ones. Let us define cL k[n], cU k[n], and cC k[n] as the Central Processing Unit (CPU) frequencies (in cycles per second) assigned for computing the kth IoT device’s task at the nth time slot, where computation occurs locally on the IoT device, on the UA V , and on the cloud tiers, respectively. The computation latency for computing the kth

[page 3]
IoT device’s task at the nth time slot on the IoT device, UA V , and cloud are represented bytL, Comp k [n], tU, Comp k [n], and tC, Comp k [n], respectively. Let wL k[n](1 − xU k[n])QL k[n] represent the amount of task (in bits) from the kth IoT device’s task that is computed locally during the nth time slot, where wL k[n] denotes the fraction of task handled locally, and xU k[n] represents the portion offloaded to the UA V . Then, let BTot[n] denote the system computed task at the nth time slot given by BTot[n] =P k∈K BTot k [n], in which BTot k [n] = wL k[n](1 − xU k[n])QL k[n] + wU k [n](1 − xC k[n])QU k[n] + wC k[n]QC k[n]. Additionally, let C indicate the number of CPU cycles required to compute each bit of task (in cycles per bit). Accordingly, the local computation latency, denoted by tL, Comp k [n], can be expressed as tL, Comp k [n] = wL k[n](1−xU k[n])QL k[n]C cL k[n] , where cL k[n] represents the computation power allocated by the kth IoT device at the nth time slot. In a similar way, the computation latency for the tasks computed by the UA V and the cloud, represented by tU, Comp k [n] and tC, Comp k [n], respectively, can be determined. Here, it is assumed that the IoT device and the UA V are capable of performing computation and task offloading concurrently. As a result, let T Tot k [n] denote the total task completion latency (in seconds) for the kth IoT device’s task during the nth time slot, expressed as T Tot k [n] = max n tL, Comp k [n], tU, Comm k [n] o + tC, Comp k [n] +max n tU, Comp k [n], tC, Comm k [n] o . C. Queueing Model Each IoT device maintains a dedicated queue to temporarily hold incoming tasks before they are computed. Similarly, both the UA V and cloud server haveK distinct queues, each assigned to a specific IoT device. In this section, we model the queue dynamics and updates at each tier (local, UA V , and cloud). Local Queueing Model: Let Ik[n] denote the number of task bits arriving at the kth IoT devices during the nth time slot. The task arrival rate for IoT devices is independent and identically distributed (i.i.d.) and is bounded by a maximum value Imax k for each device. Ensuring stability in the queues across all nodes relies on tracking queue backlogs. For each IoT device k at the nth time slot, the backlog at local tier is represented by QL k[n], initialized at QL k[0] = 0 . As such, a portion of this backlog, offloaded to the UA V , is given by xU k[n]QL k[n], and accordingly, the portion computed locally by the IoT device is wL k[n](1 − xU k[n])QL k[n]. Here, wL k[n] denotes the fraction of task computed by the IoT device itself. Thus, the IoT device queue updates as QL k[n + 1]=  QL k[n]−xU k[n]QL k[n]−wL k[n](1−xU k[n])QL k[n] + + Ik[n], where {·}+ ensures that queue values remain non-negative. UA V Queueing Model: For the UA V’s queue, which holds tasks offloaded from the kth IoT device, the backlog QU k[n] updates according to QU k[n + 1] = QU k[n]−wU k [n](1 − xC k[n])QU k[n] − xC k[n]QU k[n] + + xU k[n]QL k[n], where wU k [n] represents the fraction of tasks computed by the UA V , andxC k[n] represents the portion of tasks further re-offloaded to the cloud. Cloud Queueing Model: The cloud queue QC k[n], storing tasks re-offloaded from the UA V , updates as QC k[n + 1] =  QC k[n] − wC k[n]QC k[n] + + xC k[n]QU k[n], where wC k[n] represents the fraction of task computed by the cloud. Queue stability for each IoT device at local tier is ensured by meeting the condition ¯QL k = lim N →∞ 1 N P n∈N  QL k[n] < ∞, with similar stability conditions for the UA V and cloud queues tiers. III. P ROBLEM FORMULATION To optimize the balance between the long-term time averaged of system computed task and that of system transmission latency, we define the long-term computed task efficiency as J = lim N →∞ 1 N P n∈N BTot[n] lim N →∞ 1 N P n∈N tComm[n]. To optimize J, we adjust system parameters as transmit power matrix, denoted by P = [pk[n]]; offloading decision matrix, represented as X = [ xk[n]], where xk[n] = [ xU k[n], xC k[n]]; computing decision matrix, given by W = [wk[n]] with wk[n] = [ wL k[n], wU k [n], wC k[n]]; computing power allocation matrix, denoted by C = [ck[n]], where ck[n] = [cL k[n], cU k[n], cC k[n]]; and UA V trajectory vector, represented as ˆ q[n] = [ xU[n], yU[n]]. The problem is then formulated as follows: P1 : max P, X, W, C, ˆ q J s.t. (1a) 0 ≤ pk[n] ≤ P max, ∀k ∈ K, n ∈ N , (1b) 0 ≤ xi k[n] ≤ 1, ∀i ∈ {U, C}, k ∈ K, n ∈ N , (1c) 0 ≤ wi k[n] ≤ 1, ∀i ∈ {L, U, C}, k ∈ K, n ∈ N , (1d) 0 ≤ cL k[n] ≤ Cmax k , ∀k ∈ K, ∀n ∈ N , (1e) 0 ≤ X k∈K cU k[n] ≤ CU, max , ∀n ∈ N , (1f) 0 ≤ X k∈K cC k[n] ≤ CC, max , ∀n ∈ N , (1g) 0 ≤ ∥ˆ q[n + 1] − ˆ q[n]∥ τ ≤ V max, ∀n ∈ N , (1h) ˆ q[1] = qI , ˆ q[N] = qF , (1i) T Tot k [n] ≤ τ, ∀k ∈ K, n ∈ N , (1j) ¯Qi k < ∞, ∀i ∈ {L, U, C}, k ∈ K, (1) Here, (1a) ensures IoT devices transmit power budget at each time slot. (1b) and (1c), specify partial offloading and computing indicators at each time slot, respectively. As well, (1d), (1e) and (1f), respectively assure the computing capacity of kth IoT device, the UA V , and the cloud, up to Cmax k , CU, max and CC, max, at each time slot. In (1g), we have limited the UA V maximum velocity to V max at each time slot. Also, (1h) mandates that the UA V returns to its starting point, with qI and qF as the UA V’s starting and ending locations at each time slot. Constraint (1i) imposes a maximum allowable delay, denoted by τ, for computing each task. Finally, constraint (1j) guarantees the stability of task queues for IoT devices, the UA V , and the cloud tiers. IV. PER -TIME -SLOT TRANSFORMATIONS Due to the reliance on time-averaged values of the computeded task and transmission latency in the objective function and queue length across the local, UA V , and cloud tiers in the queue stability constraint, the problem becomes hard to

[page 4]
solve. The reason is that these values depend on information that is unavailable at the current time and cannot be reliably approximated because of the high dynamicity of the environment. To overcome this, we need to relax the long-term considerations before proceeding with a solution. Subsequently, in this section, we use an tractable approximation alongside the fractional-programming theory to relax long-term consideration of the system utility and Lyapunov function to relax long-term constraint of queue stability at each tier. Subsequently, the optimization problem P1 is converted into a sequence of pertime-slot subproblem. A. Objective Function Transformation To make the objective function J tractable at the current time, we express a tractable system utility J[n] at the nth time slot as J[n] = Pn−1 m=0 BTot[m]Pn−1 m=0 tComm[m]. Note that future information are not needed anymore to calculate this value. Subsequently, leveraging fractional programming [9], the nonfractional computed task efficiency W [n] is then given by W [n] = BTot[n] − J[n]tComm[n] that can well approximate the system utility J. B. Time-Averaged Constraint Transformation Similarly, constraint (1j) in P1 is represented in a timeaveraged form. To relax this long-term consideration, a lower value of the Lyapunov function, denoted by Φ[n], can lead to reduced backlog and decreased queue congestion, thereby enhancing the overall stability of the network [10]. Accordingly, Lyapunov function given by L (Φ[n]) = 1 2 P k∈K  QL k[n] 2 + QU k[n] 2 + QC k[n] 2 , has its onestep conditional drift upper-bounded as ∆L (Φ[n]) ≤ A[n] + B, where A[n] = PK k=1 Ak[n], and Ak[n] is given as Ak[n] = xU k[n]QL k[n]QU k[n] + xC k[n]QU k[n]QC k[n] − wL k[n](1 − xU k[n]) + xU k[n]  QL k[n] 2 − wC k[n] QC k[n] 2 − wU k [n](1 − xC k[n]) + xC k[n]  QU k[n] 2 , and B denotes a finite constant value. Subsequently, instead of minimizing Lyapunov drift directly, we can minimize its upper bound and the nearoptimal system stability can be achieved. C. Optimization Problem Transformation Considering the affect of queue stability into the nonfractional form of the objective function, we convert the original problem P1 into a series of per-time-slot optimization problem P2, where V represents a non-negative weight constant, establishing a trade-off among the queue stability and the system utility, formulated as: P2 : max P,X,W,C,ˆ q −A[n] + vW [n] s.t. (1a) − (1i) in P1, where parameter v is a scaling and trade-off factor, obtained experimentally, that ensures the minimization of A[n] and the maximization of W [n] are balanced in magnitude, preventing one term from dominating the objective function. As observed, long-term considerations of problem P1 are relaxed in problem P2. However, the system transmission latency in the objective function is fractional with respect to optimization parameters such as transmit power, computing portion and offloading indicator, rendering the problem non-convex. While a brute-force exhaustive search could theoretically find a globally optimal solution, it is impractical due to the complexity and scalability of the problem, even for moderately sized systems. Moreover, traditional approaches based on convex optimization theory [4]–[6] require complex and time-consuming transformations to achieve locally optimal solutions. However, given the dynamic nature of the MEC network, influenced by UA V mobility, a real-time solution is essential. V. P ROPOSED SOLUTION APPROACH To address the complex optimization problem P2, DRL is an effective approach as it can jointly optimize multiple variables through real-time system feedback while simultaneously learning system dynamics, without decomposing the problem into subproblems. Accordingly, we begin by modeling the system parameters as a MDP [15]. In our previous work [8], we demonstrated the feasibility of this approach using deep Qnetworks (DQN). However, DQN’s reliance on discrete action spaces limited its ability to handle the continuous optimization parameters required in this setting. Additionally, its scalability was constrained, as the action space grew exponentially with the number of IoT devices, making it impractical for largescale deployments. To overcome these limitations, we adopt the MTD3 algorithm, which efficiently manages continuous action spaces while improving adaptability in dynamic environments. Meta-learning in MTD3 enables the agent to adaptively finetune its critics based on task-specific feedback, leading to faster convergence and improved generalization across varying environments. A. MDP Model of the Problem Let us consider an edge node as the decision-maker agent, responsible for capturing system features and dynamics while optimizing parameters. In this section, we model our wireless system as an MDP, a framework in which the environment is represented by a set of states influenced by the agent’s actions with each state-action pair yielding an associated reward. By receiving this reward value, the agent learns and adapts its policy to improve system performance. These components, tailored specifically to our problem, are defined as follows. State Space: The state space, denoted as S, consists of all possible states the system can occupy at any given time. Here, the state is a minimal and sufficient representation of past information, containing everything necessary to make optimal decisions in the present and future [15]. At the nth time slot, the system’s state is given by s[n] ∈ S and expressed as s[n] = {n, sk[n], ∀k ∈ K}, where sk[n] represents the state of the kth IoT device. This device state sk[n] is detailed as sk[n] = QL k[n], QU k[n], QC k[n]  , capturing different queue conditions of the device. Action Space: The set of actions, represented by A, covers all feasible actions available for each state in the system. Here, an action is a decision variable that drives the evolution

[page 5]
of the system from one state to another, representing the choice made by the decision-maker. During the nth time slot, the action chosen is represented by a[n] ∈ A and takes the form a[n] = {ak[n], ∀k ∈ K} , where ak[n] denotes the action taken by the kth IoT device, which is defined as ak[n] =  pk[n], xk[n], wk[n], ck[n], ˆq[n]  , encompassing various control parameters for system operation. Reward: The reward function, r(s[n], a[n]) : S × A → R, assigns a numerical value that quantifies the immediate benefit or cost associated with taking an action to the combination of system state and action in a given state at a time slot [15]. As such, at the nth time slot, the reward for our system is defined by r(s[n], a[n]) = P k∈K rk(s[n], a[n]), where rk(s[n], a[n]) is the reward attributed to the kth IoT device given by rk(s[n], a[n]) = ηk[n](−Ak[n] + v1BTot k [n] − v2Zk[n]tComm k [n]), where v1 and v2 are obtained experimentally, and Zk[n] = Pn−1 m=0 BTot k [m]Pn−1 m=0 tComm k [m]. Here, ηk[n] represents how much of the available time slot remains after accounting for the total task completion latency of the the kth IoT device at nth time slot, calculated as ηk[n] = [1 − ttot k [n] τ ]+. B. MTD3 Approach To address problem P2, we propose utilizing MTD3 as detailed in Algorithm 1 . Algorithm 1 MTD3 (Meta Twin Delayed Deep Deterministic Policy Gradient) 1: Init meta-policy πϕ, meta-critics Qθ1 , Qθ2 , target nets ϕ′ ← ϕ, θ′ j ← θj 2: Init replay buffers Bi for each task Ti 3: for each meta-iteration do 4: Sample tasks {Ti}I i=1 ∼ p(T ) 5: for each task Ti do 6: Set ϕi ← ϕ, θi,j ← θj 7: for t = 1 to CIn do 8: Take action a ∼ πϕi (s) + ϵ, observe r, s′, store in Bi 9: Sample batch of size M from Bi 10: a′ ← πϕ′ (s′) + ϵ, y ← r + γ minj Qθ′ j (s′, a′) 11: Update critics: θi,j ← arg min P(y − Qθi,j (s, a))2 12: if t mod d = 0 then 13: ϕi ← ϕi + α∇ϕi J(ϕi) 14: Update targets: θ′ j ← τ θi,j + (1 − τ )θ′ j , ϕ′ ← τ ϕi + (1 − τ )ϕ′ 15: end if 16: end for 17: Compute Lmeta,i = 1 M P j P(y − Qθi,j (s, a))2 18: end for 19: Meta-update: ϕ ← ϕ − α∇ϕ P i Lmeta,i 20: θ′ j ← τ θi,j + (1 − τ )θ′ j 21: end for The per-iteration time complexity of the MTD3 algorithm is given by O(CInM((|S| + |A|)W + (L − 1)W 2 + W |A|)), where CIn denotes the number of inner loop steps per task, M is the mini-batch size, |S| and |A| are the dimensions of the state and action spaces, W is the number of neurons per hidden layer, and L is the number of hidden layers. In our system, where the state and action space dimensions scale linearly with the number of devices K (i.e., |S| ∝ K and |A| ∝ K), an increase in the number of IoT devices leads to a linear growth in |S| and |A|. This linear growth directly results in a proportional increase in the overall computational complexity, significantly affecting the algorithm’s scalability in large-scale IoT deployments. VI. N UMERICAL RESULTS In this section, we illustrate the effectiveness of the proposed resource allocation mechanism by comparing the the system performance against two benchmark schemes Deep Deterministic Policy Gradient (DDPG) [2] and Twin Delayed Deep Deterministic Policy Gradient (TD3) [12]. In the simulation, the UA V follows a trajectory that starts and ends at the center of a 1000 × 1000 m2 coverage area, within which five IoT devices are randomly distributed. Each time slot, denoted by τ, lasts for 1 second, and the incoming task for each IoT device during each time slot is randomly generated between 0 and 50 MB. The remaining simulation variables are detailed in Table 2a, unless stated otherwise. To thoroughly assess the performance of the proposed resource allocation scheme, each scenario was simulated in 50 parallel environments, each for 1000 realizations. The convergence behavior and performance of MTD3 are compared to baseline algorithms (DDPG and TD3) across various metrics in Figs. 2b-2f. In Fig. 2b, MTD3 converges faster and achieves higher rewards than DDPG and TD3, highlighting its improved sample efficiency and stability during training. This performance boost is primarily due to the integration of meta-learning techniques, which enable MTD3 to adaptively fine-tune its critic networks based on taskspecific learning dynamics. The early-stage learning curve of MTD3 shows a steep upward trend, indicating that it quickly adapts to the environment dynamics, whereas TD3 and DDPG exhibit slower, more fluctuating progress. The meta-learning component allows the agent to generalize better across varied state-action distributions, leading to more informed policy updates and accelerated convergence. Fig. 2c illustrates the effect of UA V computing capacity on computed task efficiency. As the UA V’s capacity increases, computed task efficiency (in MBps) also rises, with MTD3 outperforming DDPG and TD3 by 53.40% and 29.28%, respectively. Fig.s 2d, 2e, and 2f depict how increasing UA V computing capacity influences queue lengths at the IoT, UA V , and cloud tiers. As UA V computational resources grow, the overall queue lengths decrease, as more tasks are processed locally at the UA V , reducing the workload burden on other tiers. In Fig. 2d, higher UA V computational power enables a greater portion of tasks to be offloaded, significantly reducing local queue congestion. MTD3 demonstrates superior performance by reducing local queue lengths by 3.69% and 4.32% compared to TD3 and DDPG, respectively. Similarly, Fig. 2e highlights that as the UA V’s processing capability improves, it handles a larger share of tasks, resulting in shorter UA V queue lengths. MTD3 again outperforms TD3 and DDPG, reducing UA V queue lengths by 5.39% and 7.58%, respectively. Fig. 2f further shows that a more capable UA V relies less on cloud offloading, thereby shortening cloud queue lengths. Here, MTD3 demonstrates up to 28.96% improvement compared to DDPG and up to 21.37% improvement compared to TD3.

[page 6]
(a) Simulation parameters table 0 1000 2000 3000 4000 5000 Episode 1000 1200 1400 1600 1800 2000Cumulative reward MTD3 TD3 DDPG (b) Convergence behavior comparison 105 106 107 108 109 1010 UAV Computing Capacity (Hz) 15.0 17.5 20.0 22.5 25.0 27.5 30.0 32.5 35.0Network computed data efficiency (Mbps) DDPG TD3 MTD3 (c) Computed task efficiency 105 106 107 108 109 1010 UAV Computing Capacity (Hz) 2900 2950 3000 3050 3100 3150Local queue length (KB) DDPG TD3 MTD3 (d) Length of IoT queues 105 106 107 108 109 1010 UAV Computing Capacity (Hz) 1950 2000 2050 2100 2150 2200 2250UAV queue length (KB) DDPG TD3 MTD3 (e) Length of UA V queues 105 106 107 108 109 1010 UAV Computing Capacity (Hz) 70 80 90 100 110 120 130 140Cloud queue length (KB) DDPG TD3 MTD3 (f) Length of cloud queues Fig. 2: Simulation parameters table, convergence behaviour and performance evaluation comparison of MTD3, TD3, and DDPG across UA V computing capacities. Among the three approaches, DDPG results in the longest queues, indicating relatively inefficient workload distribution. VII. C ONCLUSIONS This study has examined the joint optimization of computed task and transmission latency, while ensuring queue stability in a UA V-assisted MEC framework from a long-term perspective. This optimization was structured in three steps relying on Lyapunov theory, MDP reformulation and MTD3. The proposed MTD3 optimization outperformed DDPG and TD3 by up to 53.40% and 29.28% in computed task efficiency, respectively. MTD3 also reduces queue lengths by 3.69%, 5.39%, and 21.37% compared to TD3, and by 4.32%, 7.58%, and 28.96% compared to DDPG at local, UA V , and cloud tiers, respectively. REFERENCES [1] K. Wang, J. Jin, Y . Yang, T. Zhang, A. Nallanathan, C. Tellambura, and B. Jabbari, “Task offloading with multi-tier computing resources in next generation wireless networks,” IEEE J. Sel. Areas Commun. , vol. 41, no. 2, pp. 306–319, Feb. 2023. [2] L. Zhang, B. Jabbari, and N. Ansari, “Deep reinforcement learning driven UA V-assisted edge computing,”IEEE Internet Things J. , vol. 9, no. 24, pp. 25449–25459, Aug. 2022. [3] L. Zhang and N. Ansari, “Optimizing the operation cost for UA V-aided mobile edge computing,” IEEE Trans. V eh. Technol., vol. 70, no. 6, pp. 6085–6093, Jun. 2021. [4] H. Hu, X. Zhou, Q. Wang, and R. Q. Hu, “Online computation offloading and trajectory scheduling for UA V-enabled wireless powered mobile edge computing,” China Commun. , vol. 19, no. 4, pp. 257–273, Apr. 2022. [5] J. Zhang, L. Zhou, Q. Tang, E. C.-H. Ngai, X. Hu, H. Zhao, and J. Wei, “Stochastic computation offloading and trajectory scheduling for UA V-assisted mobile edge computing,” IEEE Internet Things J. , vol. 6, no. 2, pp. 3688–3699, Apr. 2019. [6] Z. Yang, S. Bi, and Y . A. Zhang, “Dynamic offloading and trajectory control for UA V-enabled mobile edge computing system with energy harvesting devices,” IEEE Trans. Wireless Commun. , vol. 21, no. 12, pp. 10515–10528, Dec. 2022. [7] H. Poursiami and B. Jabbari, “On multi-task learning for energy efficient task offloading in multi-UA V assisted edge computing,” in Proc. IEEE Wireless Commun. Netw. Conf. (WCNC) , Dubai, United Arab Emirates, 2024, pp. 1–6. [8] M. F. Dehkordi and B. Jabbari, “Joint long-term processed task and communication delay optimization in UA V-assisted MEC systems using DQN,” in Proc. IEEE Mil. Commun. Conf. (MILCOM) , Washington, DC, USA, 2024, pp. 288–293. [9] W. Dinkelbach, “On nonlinear fractional programming,” Manage. Sci., vol. 13, no. 7, pp. 492–498, 1967. [10] M. J. Neely, “Stochastic network optimization with application to communication and queueing systems,” Synth. Lect. Commun. Netw. , vol. 3, no. 1, pp. 1–211, Jan. 2010. [11] C. Finn, P. Abbeel, and S. Levine, “Model-agnostic meta-learning for fast adaptation of deep networks,” in Proc. Int. Conf. Mach. Learn. (ICML) , 2017, pp. 1126–1135. [12] M. Rahmani, M. Bashar, M. J. Dehghani, P. Xiao, R. Tafazolli, and M. Debbah, “Deep reinforcement learning-based power allocation in uplink cell-free massive MIMO,” in Proc. IEEE Wireless Commun. Netw. Conf. (WCNC), 2022, pp. 459–464. [13] W. Lu, Y . Ding, Y . Gao, Y . Chen, N. Zhao, Z. Ding, and A. Nallanathan, “Secure NOMA-based UA V-MEC network towards a flying eavesdropper,” IEEE Trans. Commun. , vol. 70, no. 5, pp. 3364–3376, Mar. 2022. [14] C. You and R. Zhang, “3D trajectory optimization in Rician fading for UA V-enabled data harvesting,”IEEE Trans. Wireless Commun. , vol. 18, no. 6, pp. 3192–3207, Apr. 2019. [15] W. B. Powell, Approximate Dynamic Programming: Solving the Curses of Dimensionality . Hoboken, NJ, USA: John Wiley & Sons, 2007.
